language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bord4' ]; then cd .. && eval "mv $currentfolder generator-bord4" && cd generator-bord4; fi

